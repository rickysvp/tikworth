import { redirect } from 'next/navigation'

// DELETED: Post 18 - No TokValue CTA, readers don't know where to go after
// Redirect to monetization guide
export default function DeletedPost18() {
  redirect('/blog/tiktok-rpm-2026')
}
